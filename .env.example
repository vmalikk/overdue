# Overdue Assignment Tracker - Environment Variables

# ============================================
# Google OAuth (for Calendar Sync)
# ============================================
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
NEXTAUTH_SECRET=your-nextauth-secret-generate-with-openssl-rand-base64-32
NEXTAUTH_URL=http://localhost:3000

# ============================================
# Appwrite (Server-side Admin Access)
# ============================================
# API key with users.read, users.write, databases.read, databases.write permissions
APPWRITE_API_KEY=your-appwrite-api-key

# ============================================
# Gradescope Integration
# ============================================
# 32-byte encryption key for session tokens (generate with: openssl rand -base64 32)
GRADESCOPE_ENCRYPTION_KEY=your-gradescope-encryption-key

# ============================================
# Optional: Appwrite Storage
# ============================================
# Custom storage bucket ID (optional - uses default if not set)
# NEXT_PUBLIC_APPWRITE_BUCKET_ID=your-bucket-id
